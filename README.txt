COVE CRICKET FANTASY — SIGN-IN FIX

This version fixes the account button/auth form wiring and adds clearer Supabase connection errors.

Files: index.html, style.css, script.js

Supabase project is already configured in script.js using the browser-safe publishable key.

If Sign in still shows an error, the message in the sign-in box is the Supabase error and should tell you what needs changing in Supabase.

Important: never replace the publishable key with a service_role/secret key.
